<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.PasswordManager" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary branding colors for light theme -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryVariant</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:colorBackground">@color/colorBackground</item>
        <item name="android:textColorPrimary">@color/textColorPrimary</item>

        <!-- Status bar and navigation bar colors -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/colorPrimaryVariant</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/colorPrimary</item>
    </style>
</resources>
